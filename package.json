{
  "name": "@rdev06/bunt",
  "module": "src/index.ts",
  "main": "index.js",
  "scripts": {
    "build":"tsc && bun run build.ts",
    "postinstall": "bun run build && rm -r src & rm tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "class-validator-jsonschema": "^5.0.0",
    "mongodb": "^6.5.0",
    "reflect-metadata": "^0.2.2",
    "typedi": "^0.10.0"
  }
}